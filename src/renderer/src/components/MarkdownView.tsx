// Minimal stub — replaced by the rendered-markdown pipeline task.
export default function MarkdownView({ content }: { content: string }): React.JSX.Element {
  return <pre className="markdown-stub">{content}</pre>
}
